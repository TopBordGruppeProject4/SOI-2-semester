<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SHI.View"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity" 
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:SHI.ViewModel"
    x:Class="SHI.View.StoragePage"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:MainViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="Tilbage" HorizontalAlignment="Left" Height="73" Margin="10,10,0,0" VerticalAlignment="Top" Width="201" Command="{Binding NavigateBackCommand, Mode=OneWay}"/>
        <Button Content="Log Ud" HorizontalAlignment="Left" Height="57" Margin="876,7,0,0" VerticalAlignment="Top" Width="263" Command="{Binding LogOutCommand}"/>
        <TextBlock HorizontalAlignment="Left" Height="51" Margin="1172,10,0,0" TextWrapping="Wrap" Text="{Binding CurrentWorker.Name}" VerticalAlignment="Top" Width="189"/>
        <ListView x:Name="RawList" HorizontalAlignment="Left" Height="411" Margin="230,179,0,0" VerticalAlignment="Top" Width="225" ItemsSource="{Binding RawMaterialCatalogSingleton.Instance.RawMaterials}">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="SelectionChanged">
                    <core:InvokeCommandAction Command="{Binding SelectRawMaterialCommand}" CommandParameter="{Binding SelectedItem, ElementName=RawList}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </ListView>
        <ListView x:Name="ProductList" HorizontalAlignment="Left" Height="411" Margin="541,179,0,0" VerticalAlignment="Top" Width="225" ItemsSource="{Binding ProductCatalogSingleton.Instance.Products}">
            <interactivity:Interaction.Behaviors>
                <core:EventTriggerBehavior EventName="SelectionChanged">
                    <core:InvokeCommandAction Command="{Binding SelectProductCommand}" CommandParameter="{Binding SelectedItem, ElementName=ProductList}"/>
                </core:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </ListView>
        <TextBlock HorizontalAlignment="Left" Height="39" Margin="976,197,0,0" TextWrapping="Wrap" Text="Ændre Antal" VerticalAlignment="Top" Width="160" FontSize="22"/>
        <TextBox HorizontalAlignment="Left" Height="40" Margin="976,254,0,0" TextWrapping="Wrap" Text="{Binding ProductAmount, Mode=TwoWay}" VerticalAlignment="Top" Width="160" RenderTransformOrigin="0.476,-0.343"/>
        <Button Content="Rå Materiale" HorizontalAlignment="Left" Height="51" Margin="976,352,0,0" VerticalAlignment="Top" Width="160" Command="{Binding ChangeRawMaterialCommand}"/>
        <Button Content="Færdigt Produkt" HorizontalAlignment="Left" Height="51" Margin="976,436,0,0" VerticalAlignment="Top" Width="160" Command="{Binding ChangeProductCommand}"/>
        <TextBlock HorizontalAlignment="Left" Height="39" Margin="230,135,0,0" TextWrapping="Wrap" Text="Rå Materialer" VerticalAlignment="Top" Width="160" FontSize="22"/>
        <TextBlock HorizontalAlignment="Left" Height="39" Margin="541,135,0,0" TextWrapping="Wrap" Text="Færdige Produkter" VerticalAlignment="Top" Width="191" FontSize="22"/>
    </Grid>
</Page>
